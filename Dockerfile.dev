# dev tier: backend container with hot-reload (nodemon/ts-node)
FROM node:26-alpine

WORKDIR /usr/src/app

ENV PUPPETEER_SKIP_DOWNLOAD=true
COPY package*.json ./
RUN npm install --ignore-scripts

COPY tsconfig.json ./
COPY src ./src

EXPOSE 8080

CMD ["npm", "run", "dev"]
