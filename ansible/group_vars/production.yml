---
node_env: production
enable_webhook_test: false
webhook_test_token: ""

# Replace with your production secrets / vault variables
# Use `ansible-vault encrypt_string` for sensitive keys
line_channel_id: "{{ vault_line_channel_id | default('') }}"
line_channel_secret: "{{ vault_line_channel_secret | default('') }}"
line_channel_access_token: "{{ vault_line_channel_access_token | default('') }}"
admin_user_id: "{{ vault_admin_user_id | default('') }}"
line_agent_name: "Cloudnex Assistant"

# Odoo Integration
odoo_url: "{{ vault_odoo_url | default('') }}"
odoo_db: "{{ vault_odoo_db | default('') }}"
odoo_username: "{{ vault_odoo_username | default('') }}"
odoo_api_key: "{{ vault_odoo_api_key | default('') }}"
odoo_rpc_timeout_ms: 7000
odoo_read_retry_attempts: 3
odoo_write_retry_attempts: 2

# Operations Token
ops_api_token: "{{ vault_ops_api_token | default('') }}"

# Public URL
public_base_url: "https://your-production-domain.com"

# Google Cloud
google_cloud_project: "{{ vault_google_cloud_project | default('') }}"
google_cloud_location: "us-central1"
google_ai_studio_api_key: "{{ vault_google_ai_studio_api_key | default('') }}"
ai_off: false

# Feature Flags
groupbuy_enabled: false
groupbuy_rollout_percent: 0
enable_demo_control_panel: false
